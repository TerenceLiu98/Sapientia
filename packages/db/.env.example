# Used by drizzle-kit (db:generate, db:studio) and the migrate script.
# For local dev this matches apps/api/.env.example.
DATABASE_URL=postgresql://sapientia:dev_password@localhost:5432/sapientia_dev
